import { createStyles, makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => {
  return createStyles({
    root: {
      padding: "1.5em 3em",
      margin: "auto"
    }
  });
});
